class Agree < ActiveRecord::Base
  attr_accessible
  
  belongs_to :user
  belongs_to :post

	fires :new_agree, :on                 => :create,
                    :actor              => :user,
										:subject						=> :post
end
